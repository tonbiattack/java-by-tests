# Java by Tests — デザイン検討

## アプローチ候補

### 1. 証明の作業台
**Very Brief Intro**: 研究室のワークベンチと技術書の余白を組み合わせ、コードと検証結果に集中できる静かなダークUI。学習者が「読む」のではなく「確かめる」感覚を持てる。

**Probability**: 0.07

### 2. 黒板ノートの注釈集
**Very Brief Intro**: チョークの注釈、付箋、手書きの矢印を控えめに使う、講義ノート風の教育体験。概念のつながりを強く印象づける。

**Probability**: 0.04

### 3. 構文の標本箱
**Very Brief Intro**: Javaの各挙動をラベル付きの標本として収める、博物館の収蔵庫を思わせる構成。コンテンツの探索性と分類を優先する。

**Probability**: 0.09

---

## 選定：証明の作業台

### Design Movement
**Swiss International Typographic Style** の情報階層と、プログラマ向けIDEの精密な作業空間を融合する。装飾よりも明快な構造を優先しつつ、検証の「生きた記録」らしい温度を残す。

### Core Principles
1. **Source と Test を同格に置く。** すべての主題は、左右に対になるふたつのコード面から始まる。
2. **検証できた事実を視覚化する。** 成功状態は鮮明な緑の細線、短いラベル、チェック記号で簡潔に示す。
3. **余白は解説の一部。** 長文を避け、段階的な注釈と見出しで視線をコードへ戻す。
4. **学習の現在地を常に示す。** 左レール、章番号、読了状態によって、内容の位置関係を失わせない。

### Color Philosophy
背景は紙の白ではなく、深い**コードインク**（ほぼ黒の青緑）にする。これは長いコード閲覧時の眩しさを避け、エディタの集中感を作るためである。文字は真白ではなく少し温度を下げた灰白。構文色は用途を限定し、検証済みの事実だけをライム寄りの緑で示す。暖色のアンバーは注意・例外のみに使う。

### Layout Paradigm
中央揃えのランディングページではなく、**固定の学習レール + 可変のコード作業面**を採用する。デスクトップでは左に章ナビゲーションを置き、右側を「章のメタ情報」「50:50の Source/Test 作業台」「解説ノート」の順に流す。各コード面の上端が揃い、中央の細い仕切りがふたつのファイルの関係を表す。モバイルではレールを折りたたみ、Source → Test → Notes の単一列に再構成する。

### Signature Elements
1. **検証トレース**: Source と Test の境界をつなぐ細い緑のラインと小さなチェックノード。
2. **ファイルタブ**: 拡張子の色と小型のファイルアイコンを含む、IDEを思わせる矩形タブ。
3. **マージンノート**: 解説の先頭に章内通し番号を付け、細線で本文へ接続する注釈様式。

### Interaction Philosophy
操作は作業道具のように即時かつ控えめに応答する。コピー、章移動、テーマ切替、モバイルメニューは明確なフォーカスリングを持ち、状態変化は色とラベルの両方で伝える。装飾的な操作を増やさず、コードを読む行為を妨げない。

### Animation
ページ初期表示では章メタ情報、左右のコード面、解説ノートを 50ms 間隔で短くフェード・スライドインさせる。ホバー時はカード全体を浮かせず、境界線とタブのコントラストだけを変化させる。コピー完了時にはアイコンと文言を 160ms で切り替える。すべての動きは `prefers-reduced-motion` で停止可能にし、300msを超えない。

### Typography System
見出しには **Space Grotesk** を使用し、機械的すぎない太い幾何学感で章の構造を示す。本文は **Noto Sans JP** で日本語の可読性を確保し、コードは **IBM Plex Mono** で整列と記号の識別性を優先する。見出しは太字・低めの字間、本文は標準ウェイト、コードは13〜14px以上を保つ。

### Brand Essence
**Java by Tests は、Javaの挙動をJUnitで一つずつ証明しながら理解したい開発者のための、実行可能なリファレンスである。**

性格: **精密、静謐、実証的**

### Brand Voice
結論を先送りせず、確認できる観測事実から簡潔に述べる。見出し・CTA・マイクロコピーは「理解した気になる」表現ではなく、テストで確かめる行動を促す。

例:

> 返り値ではなく、境界条件を確かめる。

> この一行が、例外の契約を証明する。

### Wordmark & Logo
ロゴマークは、角括弧を思わせる二本の縦線の間に、JUnitのチェックを抽象化した斜めの一本線を置く。左から右へ読む Source → Test の流れと、検証済みの印を一つの小さな記号として表現する。ワードマークは Space Grotesk のカスタム字間で、`JAVA` を細く、`/TESTS` を太く組む。

### Signature Brand Color
**Verification Green — #B6F04A**。検証済みの状態にのみ用いる、Java by Tests 固有の鮮やかな黄緑。

## Style Decisions

- Verification Green は、検証ノード、assertion、成功状態、主要な「検証を始める」導線に限定し、カテゴリや受動的なメタデータには使わない。
- 教材ページでは Source/Test のコード比較面を最大の証拠オブジェクトとし、常に中央の検証トレースでつなぐ。
- 右余白は装飾画像ではなく、番号付きの証拠ノート、ファイル参照、または検証トレースに用いる。
- 初見の画面では、題名よりSource/Testの作業台が主たる証拠オブジェクトとして読める大きさと余白を優先する。
- Verification Green（#B6F04A）は検証トレース、チェック、assertion、観測結果、主要な検証操作に限定し、受動的なコンテナ境界と現在地表示には中立的なコードインク色を用いる。
- 解説部は番号・細線・ノードでSource/Testと結果へ接続した証拠ノートとして扱い、独立した記事カードに見せない。
